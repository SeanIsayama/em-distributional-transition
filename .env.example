# Optional — defaults work without any of these.
# EM_DATA_DIR=/mnt/sagemaker-nvme/em-data     # default: ~/.cache/em-distributional-transition
# EM_OUTPUT_DIR=outputs                        # default: ./outputs
# HF_HOME=/mnt/sagemaker-nvme/hf_cache         # default: ~/.cache/huggingface

# Required for judging
OPENROUTER_API_KEY=

# Required for fetching datasets and artifacts
HF_TOKEN=
